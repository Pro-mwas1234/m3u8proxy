# The name of your worker (appears in your .workers.dev URL)
name = "m3u8-proxy"

# Points Cloudflare to your specific 'cf' logic file
main = "m3u8proxy(cf_worker).js"

# Ensures your worker uses the latest stable engine features
